import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";
import "./../public/styles.css";

ReactDOM.render(<App />, document.getElementById("root"));
// Now in the previos module we put everything together in the app.jsx
// But we know that it is not the way development gets carried out.
// For that reason, we will be making new components.
